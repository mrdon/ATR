<atlassian-plugin key="${atlassian.plugin.key}" name="${pom.name}" pluginsVersion="2">
    <plugin-info>
        <description>${pom.description}</description>
        <version>${pom.version}</version>
        <vendor name="${pom.organization.name}" url="${pom.organization.url}"/>
        <bundle-instructions>
            <Export-Package>
                com.atlassian.templaterenderer.velocity.one.five,
                org.apache.velocity.tools.*; version=1.3,
                org.apache.velocity.*; version=1.5
            </Export-Package>
        </bundle-instructions>
    </plugin-info>

    <!-- SAL components -->
    <component-import key="i18nResolver" interface="com.atlassian.sal.api.message.I18nResolver"/>

    <component key="velocity-1.5-template-renderer-factory" class="com.atlassian.templaterenderer.velocity.one.five.internal.VelocityTemplateRendererFactoryImpl" public="true">
        <interface>com.atlassian.templaterenderer.TemplateRendererFactory</interface>
        <interface>com.atlassian.templaterenderer.velocity.one.five.VelocityTemplateRendererFactory</interface>
    </component>
</atlassian-plugin>